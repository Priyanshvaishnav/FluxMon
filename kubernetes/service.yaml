apiVersion: v1
kind: Service
metadata:
  name: dumsumoh-ser
spec:
  type: NodePort
  selector:
    app: dumsumoh
  ports:
    - protocol: TCP
      port: 8050
      targetPort: 8050
      nodePort: 30080
